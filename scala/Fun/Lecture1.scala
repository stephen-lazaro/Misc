//Lecture 1.scala
//Pay attention to the substitution model
//Even if this stuff seems easy.

object LectureOne extends App {
	def square(x: Double) = x * x
	def sumOfSquare(x : Double, y : Double) = x * x + y * y
}

//Scala is a lambda language.

