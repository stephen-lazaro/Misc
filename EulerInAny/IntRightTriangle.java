//Right Angle Triangles
public class RightTriangle {
	int a,b,hyp;
	public static RightTriangle(int n, int m) {
		double squarerootsum = Math.sqrt(n*n + m*m)
		if ((int) squarerootsum == squarerootsum) {
			this.a = n;
			this.b = m;
			this.hyp = squarerootsum;
		}
		else {
			this.a = 0;
			this.b = 0;
			this.hyp = 0;
		}
	}

	private class partition(int n, int m) {
		
	}
}